# TrueNAS WebSocket URL (new JSON-RPC API)
TRUENAS_URL=ws://your-truenas-ip/api/current

# TrueNAS Authentication Options (choose one):

# Option 1: API Key only
# TRUENAS_API_KEY=your-api-key-here

# Option 2: Username + API Key (recommended for user-specific access)
# TRUENAS_USERNAME=your-username
# TRUENAS_API_KEY=your-api-key-here

# Option 3: Username + Password (use with caution)
# TRUENAS_USERNAME=your-username
# TRUENAS_PASSWORD=your-password

# Backend Server Authentication Configuration
# Authentication mode: none, api_key, jwt, basic
AUTH_MODE=api_key

# API Key authentication
API_KEY=your-secure-api-key

# JWT authentication (if AUTH_MODE=jwt)
# JWT_SECRET=your-jwt-secret
# JWT_ISSUER=mynas-manager

# Basic authentication (if AUTH_MODE=basic)
# BASIC_AUTH_USERNAME=admin
# BASIC_AUTH_PASSWORD=secure-password

# TrueNAS Connection Heartbeat Configuration
TRUENAS_HEARTBEAT_INTERVAL=30 # seconds
TRUENAS_RECONNECT_DELAY=5 # seconds
TRUENAS_MAX_RECONNECT_ATTEMPTS=10

# Session-based Authentication Mode
# Set to 'true' to enable TrueNAS user authentication
# In this mode, users log in with their TrueNAS credentials
# and each user gets their own TrueNAS connection
USE_SESSION_AUTH=false

# Session Configuration (when USE_SESSION_AUTH=true)
SESSION_TIMEOUT=86400 # 24 hours in seconds
SESSION_INACTIVITY_TIMEOUT=900 # 15 minutes in seconds